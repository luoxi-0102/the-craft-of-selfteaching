Part1-E-1
一、学习
（1）函数
a = abs（-3.1415）
a
3.1415
abs 函数名、-3.1415 参数、3.1415 返回值

判断是否是质数的程序，可以被我们写成子程序，方便后续的直接调用
def 定义（使用定义函数时，程序的输入和输出是对于代码，所以需要return的是false和true，询问了ai，理解没有问题） 

（2）补充
语句块是否能直接用冒号来判断？最好不要
注：一个python文件中，tab和空格的效果相同，所以最好统一进行使用，不要混搭

操作符：
% 算余数
+、-、*、/ 加减乘除
// 商（17//3  商计算的结果为5）
** 幂

x += 1 相当于x=x+1（进行数学运算，上述的操作符都可以进行这样的运算）
而代码中的x=x+1 是赋值的含义

二、词汇
function name 函数名
parameters 参数
return value 返回值
call 调用
sub-program 子程序
statements 语句

三、本章概念
数据：整数、布尔值；操作符；变量、赋值；表达式
函数、子程序、参数、返回值、调用
流程控制、分支、循环
算法、优化
程序：语句、注释、语句块
输入、处理、输出
解释器